/// @desc Set current speed values

current_speed = 0;

function reset_speed() {
	current_speed = initial_speed;
}

// TODO: picking and serving

starting_point = x;
reset_speed();
yspeed = 0;
xspeed = current_speed;